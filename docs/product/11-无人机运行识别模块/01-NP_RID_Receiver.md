# 无人机运行识别接收器

## 产品说明

### 简介

NP-RID-Receiver是NextPilot推出的一款广播式无人机运行识别接收器，满足GB 46750-2025标准要求，支持WiFi或蓝牙广播信号接收和解析，最多可接收50个无人机广播信号，可通过串口输出接收数据至监管软件系统，满足对无人机飞行监管需求。

该产品有如下几个特点：

- 串口输出协议可设置，支持MAVLink协议或GB 46750协议，通过参数选择；
- 用户可通过网页查看已接收到的无人机运行标识、设备运行状态；
- 提供串口控制台，用户通过串口上位机即可输入命令，实现参数设置、重启、任务运行状态监控；
- 支持OTA升级，可在官网下载最新固件，通过网页升级；

购买途径：[首页-NextPilot-淘宝网](https://shop103678810.taobao.com/category.htm?spm=pc_detail.30350276.shop_block.dshopinfo.52f17dd6b1ptE2)

### 产品功能

- 具备无人机广播数据接收能力，支持新国标GB46750-2025；
- 具备数据输出能力，可通过串口输出，支持MAVLink协议或GB46750协议；
- 具备OTA升级能力，可通过网页上传并升级固件；
- 具备参数设置能力，可通过调试串口快速设置参数，控制设备行为与功能；


### 规格参数

- 主控芯片：ESP32-S3；
- 接口：2个串口、1个USB、1个CAN；
- 支持接收信号类型：WIFI广播信标帧、蓝牙5；
- 最大信号接收数量：50；
- 数据处理时间：≤20ms；
- 信号接收动态范围：≥74dB；
- 输出数据格式：MAVLink(OpenDroneID)/GB46750；

### 接口说明

设备主要包括如下几个接口：

- USB串口：也叫调试串口/控制台串口，波特率115200（不可改），用于查看运行状态、配置参数；

- 串口1：默认数据输出串口，默认波特率115200（可改），输出接收到的无人机信息，数据协议根据参数CFG_PROTOCOL确定；
- 串口2：数据输出串口，备用，参数CFG_UART为2时有效；
- CAN：暂时未用。

## 教学视频

视频教程请观看NextPilot发布的B站视频。

## 快速使用

### 通过输出串口获取数据

#### 设备连接

产品默认通过串口1输出数据，串口1为TTL电平，默认波特率115200，同时可通过该串口连接5V电源进行供电。

#### 选择输出协议

支持如下两种协议输出：

- GB46750协议：默认，也就是将收到的数据“透传”出来，详细请参考[国家标准|GB 46750-2025](https://openstd.samr.gov.cn/bzgk/std/newGbInfo?hcno=56E7666BF313B2F51A747F2BEEE4F896)。

- MAVLink协议：将收到的数据转成一系列OpenDroneID消息，详细请参考[MAVLINK Common Message Set (common.xml) | MAVLink Guide](https://mavlink.io/zh/messages/common.html)。

>由于国标协议内容与MAVLink协议内容并不完全一致，故为了能够通过MAVLink输出，需要借用MAVLink消息字段进行传输，稍微不同的几个字段对应如下表：
>
>| 消息                           | 原始字段            | 映射GB后的含义  |
>| ------------------------------ | ------------------- | --------------- |
>| OPEN_DRONE_ID_LOCATION (12901) | status              | 运行状态        |
>| OPEN_DRONE_ID_BASIC_ID (12900) | uas_id              | 实名登记号后8尾 |
>| OPEN_DRONE_ID_SYSTEM (12904)   | classification_type | 无人机分类      |
>|                                | category_eu         | 运行类别        |

### 通过网页查看数据

#### 连接热点 {#连接热点}

启动WiFi热点后，通过笔记本连接该热点（热点名NP-RID-Receiver，密码nextpilot）。

> 默认上电后启动热点，如果已经关闭，需要设置参数WIFI_AP_ENABLE=1，然后重启。

#### 网页说明

网页包括系统状态、无人机列表、参数说明、固件升级四个内容。其中：

- 无人机列表：显示了当前通过广播接收到的无人机信息；

- 参数说明：显示当前所有参数，关于参数具体说明请参考[参数汇总](#参数汇总)；
- 固件升级：可以升级程序以及网页，具体升级说明请参考[OTA升级](#OTA升级)。

![网页界面1](imgs\网页界面1.png)

### 启动WiFi广播接收

产品出厂默认无法通过WiFi广播接收数据，必须要关闭WiFi热点。

### 通过USB串口查看状态、配置参数

#### 说明

USB串口为调试串口/控制台串口，可通过该串口与设备进行交互。

#### 操作步骤

直接使用Type-C数据线，连接产品**USB串口**至计算机，打开串口调试上位机软件（如MobaXterm、JCom等），选择端口号，设置波特率115200。通过串口调试上位机输入相关命令即可进行参数查看、参数设置、设备重启等操作。

![image-20260804200731817](imgs\image-20260804200731817.png)

#### 常用命令

包括如下几类命令：

- 帮助命令：help
- 重启设备：reboot
- 参数命令：详见[参数命令](#参数命令)

> **注意，命令后面必须加回车才有效！！！**

常用命令有：
```bash
# 关闭WiFi热点：
param set WIFI_AP_ENABLE 0
# 开启WiFi热点：
param set WIFI_AP_ENABLE 0
# 设置输出协议为GB46750
param set CFG_PROTOCOL 0
# 设置输出协议为MAVLink
param set CFG_PROTOCOL 1
# 设置数据输出串口波特率
param set CFG_BAUD 115200
```

详细请参考[参数汇总](#参数汇总)。

## 设置参数

### 说明

可通过**USB串口**灵活设置参数进而控制设备运行逻辑，如设置数据输出串口、启动WiFi热点、启动打印信息，极大方便用户在使用过程中进行测试。

### 操作步骤

参考[通过USB串口查看状态、配置参数](#通过USB串口查看状态、配置参数)。

### 参数命令 {#参数命令}

相关参数命令如下表所示：

| 操作                 | 命令                     | 示例                                                         |
| -------------------- | ------------------------ | ------------------------------------------------------------ |
| 查看所有参数         | param list               | 查看所有参数：param list                                     |
| 重置参数为出厂默认值 | param reset              | 重置参数：param reset                                        |
| 查看指定参数         | `param get [name]`         | 查看数据输出串口波特率：param get CFG_BAUD                   |
| 设置参数             | `param set [name] [value]` | 关闭WiFi热点：param set WIFI_AP_ENABLE 0<br />设置输出协议为MAVLink：param set CFG_PROTOCOL 1<br />设置数据输出串口波特率：param set CFG_BAUD 115200 |

### 参数汇总 {#参数汇总}

所有参数说明如下表：

| 参数           | 描述                                                         | 备注                                                         |
| -------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| WIFI_AP_ENABLE | 开启/关闭WiFi热点功能<br />0：关闭<br />1：开启（**默认**）  | WiFi热点开启（AP模式）后无法接收广播数据（蓝牙一直可以接收广播数据），出厂默认开启，方便用户监控产品运行状态。<br />如需使用WiFi接收，请在 |
| CFG_UART       | 选择数据输出串口<br />1：UART1（**默认**）<br />2：UART2     | 只能设置为1或2                                               |
| CFG_BAUD       | 串口波特率，（**默认**115200）                               |                                                              |
| CFG_PROTOCOL   | 数据输出协议格式<br />0：GB46750（**默认**）<br />1：MAVLink，OpenDroneID相关消息 |                                                              |
| DBG_BT_OPTION  | 蓝牙接收调试选项<br />0：不通过串口输出调试信息（**默认**）<br />1：通过串口打印AD Structure信息（广播者MAC、SID、RSSI、数据包长度等） | 默认通过UART0打印，根据这些可快速查看周围蓝牙信号源信息      |
| DBG_DS_OPTION  | 接收到的无人机状态数据调试选项<br />0：不通过串口输出调试信息（**默认**）<br />1：打印无人机身份信息，包括唯一识别标识码、实名登记、运行类别、分类、运行状态<br />2：打印遥控站信息，<br />4：打印无人机位置信息，包括时间戳、坐标类别、经纬度、航迹角、地速、相对高度、大地高度、气压高度<br />8：打印精度信息，包括水平位置精度、垂直精度、速度精度、时间精度 | 默认通过UART0打印，根据这些可快速查看周围无人机广播信息。如果需要显示多组信息，只需要将对于数字相加即可，例如3=1+2，表示会打印无人机身份信息+遥控站信息 |



## OTA升级 {#OTA升级}

### 下载最新固件

进入[NextPilot官网](./固件下载.md)下载固件。

### 连接热点

启动WiFi热点后，通过笔记本连接该热点（热点名NP-RID-Receiver，密码nextpilot）。

> 默认上电后启动热点，如果已经关闭，需要设置参数WIFI_AP_ENABLE=1，然后重启。

### 升级固件app

连接热点后，在浏览器输入http://192.168.4.1，在最下方**固件(app)**下点击`选择文件`。

![image-20260804201752476](imgs\image-20260804201752476.png)

在打开的对话框中选择下载的**app.bin**固件，然后点击`更新固件`。

更新完成后会自动重启！

### 升级网页

在**网页(www)**下点击选择文件，选择下载的**www.bin**文件，然后点击`更新网页`，更新成功后刷新网页即可。

## 注意事项

1. **默认开启WiFi热点，如需要关闭，请设置（WIFI_AP_ENABLE=0）；**
2. **开启WiFi热点后，但只可通过蓝牙接收外部无人机广播数据，将无法继续接收WiFi广播信息；**
3. **如要进行OTA升级，确保WiFi热点已打开（WIFI_AP_ENABLE=1）、开启OTA（OTA_ENABLE=1）；**